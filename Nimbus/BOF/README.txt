Service 4: Batch Offline Features
----------------------------------
POST a JSON payload with entities and desired features.
The API returns point‑in‑time correct features.

Input:  sample_input.json (the POST body)
Output: sample_output.json (API response)
